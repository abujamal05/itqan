---
import Base from '../../../layouts/Base.astro';
import VerifyPage from '../../../components/pages/VerifyPage.astro';
import { t } from '../../../i18n';

const s = t('en');
---

<Base title={`${s.verify.title} | ${s.site.name}`} description={s.verify.metaDescription}>
  <VerifyPage locale="en" />
</Base>
