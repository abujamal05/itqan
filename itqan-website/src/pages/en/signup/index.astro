---
import Base from '../../../layouts/Base.astro';
import SignupPage from '../../../components/pages/SignupPage.astro';
import { t } from '../../../i18n';

const s = t('en');
---

<Base title={`${s.signup.title} | ${s.site.name}`} description={s.signup.metaDescription} chat={false}>
  <SignupPage locale="en" />
</Base>
